{
  "name": "minqlx-runtime",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bundle": "bun build src/main.ts --outdir ../bin/minqlx --target bun && bun build ../plugins/*.ts --outdir ../bin/minqlx/plugins --target bun",
    "typecheck": "tsgo --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "@typescript/native-preview": "latest"
  }
}
